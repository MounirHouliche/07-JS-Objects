// Creating objects & Accessing Properties

const person ={
    name: "Bob",
    age: 30,
    favouriteColour: "red",
};

console.log(person.name);
console.log(person.age);
console.log(person.favouriteColour);


// Adding Properties

const persons ={
    name: "Bob",
    age: 30,
    favouriteColour: "red",
};

persons.favouriteFood = "pizza";
console.log(persons.favouriteFood)